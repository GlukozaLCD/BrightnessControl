using Xunit;

// Несколько тестов в этом проекте обращаются к одним и тем же физическим
// мониторам по DDC/CI — это общая шина I2C, конкурентный доступ из
// параллельных тестовых потоков ломает чтение/запись. Тесты должны идти
// строго по очереди.
[assembly: CollectionBehavior(DisableTestParallelization = true)]
